#!/bin/bash

# Write a script that reads a text file and generates a list of words along with their frequencies. Bonus points for sorting the list by frequency.
# Develop a script that takes a text file and replaces all occurrences of a specific word or phrase with another word or phrase.
# Write a script that checks whether a given word or phrase is a palindrome (reads the same forwards and backwards).
# Develop a script that reverses the order of characters in each line of a text file.

#Automate Gmail configuration in Linux server, prompt user for credentials.
#Practice sed editor.
#Monitor RAM usage through free command and send email if it is more than threshold.
#You can continue configuring roboshop components using shell script.